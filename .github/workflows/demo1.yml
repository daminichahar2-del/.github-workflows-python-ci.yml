name: Hello CI

on: [push]

jobs:
  run-echo:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Say Hello
        run: echo "🎉 GitHub Actions is working!"
